[package]
name = "utils"
version = "0.1.0"
edition = "2024"

[dependencies]
wgpu = "28.0.0"
bytemuck = { version = "1.24.0", features = ["derive"] }
glam = { version = "0.31.0", features = ["serde", "bytemuck"] }
